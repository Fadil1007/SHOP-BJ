{% extends 'admin/base.html.twig' %}

{% block title %}Nouveau Slide - Administration{% endblock %}

{% block body %}
<div class="container-fluid px-4">
    <h1 class="mt-4">Ajouter un slide</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a href="{{ path('admin_index') }}">Tableau de bord</a></li>
        <li class="breadcrumb-item"><a href="{{ path('admin_carousel_index') }}">Carousel</a></li>
        <li class="breadcrumb-item active">Nouveau slide</li>
    </ol>
    
    {% for flash_error in app.flashes('error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}
    
    <div class="card mb-4">
        <div class="card-header">
            <i class="fas fa-plus me-1"></i>
            Formulaire d'ajout
        </div>
        <div class="card-body">
            {{ include('admin/carousel/_form.html.twig') }}
            
            <a href="{{ path('admin_carousel_index') }}" class="btn btn-secondary mt-3">
                <i class="fas fa-arrow-left me-1"></i> Retour à la liste
            </a>
        </div>
    </div>
</div>
{% endblock %}