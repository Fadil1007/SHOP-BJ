{% extends 'base.html.twig' %}

{% block title %}Email envoyé - SHOP BJ{% endblock %}

{% block body %}
<section class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h1 class="h4 mb-0">Email envoyé</h1>
                </div>
                <div class="card-body text-center">
                    <div class="mb-4">
                        <i class="fas fa-envelope fa-3x text-primary mb-3"></i>
                        <h2 class="h4">Vérifiez votre boîte de réception</h2>
                    </div>
                    
                    <p class="mb-4">
                        Si un compte existe pour l'adresse email que vous avez fournie, un email vient d'être envoyé contenant un lien de récupération de mot de passe.
                        Ce lien expirera dans 1 heure.
                    </p>
                    
                    <div class="alert alert-info">
                        <p class="mb-2">
                            <strong>Remarque importante :</strong> Si vous n'avez pas reçu l'email après quelques minutes :
                        </p>
                        <ul class="text-start mb-0">
                            <li>Vérifiez votre dossier spam/indésirables</li>
                            <li>Assurez-vous que votre adresse email est correcte</li>
                            <li>Si le problème persiste, contactez notre support à <strong>shopbj04@gmail.com</strong></li>
                        </ul>
                    </div>
                    {# Assurez-vous qu'aucune variable n'est affichée accidentellement ici #}
                    
                    <p class="text-muted">
                        Si vous ne recevez pas d'email, veuillez vérifier votre dossier spam ou 
                        <a href="{{ path('app_forgot_password_request') }}">réessayer</a>.
                    </p>

                    <div class="mt-4">
                        <a href="{{ path('app_login') }}" class="btn btn-outline-primary">
                            <i class="fas fa-arrow-left me-2"></i>Retour à la connexion
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}